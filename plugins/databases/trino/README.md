# Trino Database Plugin

Use this plugin to connect to a Trino database and run queries.

## Configurazione rapida

- Driver: `io.trino.jdbc.TrinoDriver`
- URL: `jdbc:trino://host:port/catalog/schema`
- In the "Database name" field use `catalog/schema`.
